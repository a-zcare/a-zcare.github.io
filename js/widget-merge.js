(()=>{const view=document.querySelector('#phoneView');if(!view)return;let skipping=false,lastDirection=1;const isClockOnly=()=>!!view.querySelector('#widgetClock')&&!view.querySelector('.widget-weather-time');const isWeather=()=>view.querySelector('.widget-content small')?.textContent?.includes('RIGA · WEATHER');const mergedHtml=()=>`<div class="widget-weather-time"><div class="weather-side"><span class="widget-symbol">☁</span><div><small>RIGA · WEATHER</small><strong>18°</strong><p>Partly cloudy · 14° / 20°</p></div></div><div class="time-side"><small>LOCAL TIME</small><strong id="mergedWidgetClock"></strong><p id="mergedWidgetDate"></p></div></div>`;function updateTime(){const now=new Date(),clock=view.querySelector('#mergedWidgetClock'),date=view.querySelector('#mergedWidgetDate');if(clock)clock.textContent=new Intl.DateTimeFormat(undefined,{hour:'2-digit',minute:'2-digit'}).format(now);if(date)date.textContent=new Intl.DateTimeFormat(undefined,{weekday:'short',day:'numeric',month:'short'}).format(now)}function mergeWeather(){if(!isWeather())return;const content=view.querySelector('.widget-content');if(content&&!content.querySelector('.widget-weather-time'))content.innerHTML=mergedHtml();updateTime()}function skipClock(direction=lastDirection){if(!isClockOnly()||skipping)return false;skipping=true;const btn=view.querySelector(`[data-widget="${direction<0?'-1':'1'}"]`);if(btn)btn.click();skipping=false;return true}function normalize(){const dots=view.querySelector('.widget-footer div');if(dots){const clockDot=dots.querySelector('[data-widget-set="1"]');if(clockDot)clockDot.hidden=true}if(isClockOnly()){if(!skipClock(lastDirection))return}else mergeWeather();updateTime()}document.addEventListener('click',e=>{const arrow=e.target.closest?.('[data-widget]');if(arrow)lastDirection=Number(arrow.dataset.widget)||1;const clockDot=e.target.closest?.('[data-widget-set="1"]');if(clockDot){e.preventDefault();e.stopImmediatePropagation();const weatherDot=view.querySelector('[data-widget-set="0"]');if(weatherDot)weatherDot.click();return}setTimeout(normalize,0)},true);setTimeout(()=>{if(isClockOnly()){lastDirection=-1;skipClock(-1)}normalize()},0);setInterval(updateTime,1000)})();